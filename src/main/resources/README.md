Application.properties is being used to store api keys.
For the program to work add the following keys with valid api keys.

APOD_KEY

Optionally if you want to start the server on a port other than 8080 add...
ser.port=[PORT_NUM]